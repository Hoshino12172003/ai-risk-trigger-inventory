# External Optimization Oracle

The Paper 2 optimizer is not copied into this repository. If integration is
required later, it will retain fixed-commit provenance and may use a Git
submodule, package dependency, or command interface. The integration mechanism
will not be selected until the formal protocol is frozen.
